
# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv  - type: web
    name: flask-poetry
    env: python
    plan: starter
    buildCommand: poetry install
    startCommand: gunicorn app:app --chdir ./poetry_flask
    autoDeploy: false

# pre-commit
.pre-commit-config.yaml


